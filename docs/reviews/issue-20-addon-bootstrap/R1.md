# R1 レビュー結果

- 対象: 作業ツリーの未コミット差分（`git diff main`） vs `main@7b04c83eb9f4ec861d0f9aed795749756404f805`
  （このブランチはまだコミットしておらず `HEAD == main`。Issue #20 の全作業はワーキングツリー上にある）
- 判定: **APPROVE**

New Critical/High/Medium なし。

## 指摘

なし。

## 対象外指摘（差分範囲外）

なし。

## Low（backlogへ）

- [R1-L1][Low][plugins/saai-knowledge-for-woocommerce/tests/test-bootstrap.php:648] `test_missing_woocommerce_notice_is_capability_gated_and_escaped()` の
  `assertStringContainsString( esc_html( Bootstrap::notice_message( 'missing_woocommerce' ) ), $output )` は、
  `notice_message()` が返す文字列がHTML特殊文字を含まない静的文言のため、`esc_html()` 呼び出し自体が
  無くても（未エスケープでも）このアサーションは通ってしまう。権限ゲート（非権限ユーザーへの非表示）の検証は
  有効だが、エスケープの検証としては実効性が薄い。現状これらの通知文言に外部入力は一切混入しないため
  実害は無いが、将来動的な値（バージョン番号以外）を通知に含める変更が入った際に回帰を検知できない。
- [R1-L2][Low][plugins/saai-knowledge-for-woocommerce/tests/test-bootstrap.php:613] `test_boot_is_idempotent_and_stores_base_plugin()` の
  `assertNull( Plugin::instance(), ... )` という前提は、「PHPUnitのブートストラップ時点でWooCommerceが
  読み込まれておらず `requirements_status()` が `missing_woocommerce` になるため `Plugin::boot()` が
  まだ実行されていない」という現在のテスト環境構成に暗黙に依存する。将来 M5-2 以降でPHPUnit環境に
  WooCommerceを導入した場合、この前提が崩れてテストが失敗しうる（メッセージ付きなので原因は追いやすい）。

## サマリ

- 指摘件数: 0（Critical/High/Medium）+ Low 2件（backlog送り）
- 修正件数: 0
- backlog送り: 2件
