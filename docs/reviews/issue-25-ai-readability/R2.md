# R2 レビュー結果（検証ラウンド）

- 対象: R1で指摘した [R1-1] の修正差分のみ
  （`plugins/saai-knowledge/includes/class-markdown-output.php` の `maybe_serve()` への
  `post_password_required()` チェック追加、および
  `plugins/saai-knowledge/tests/test-markdown-output.php` への回帰テスト追加）。
  未コミットのため、コミットSHAではなくファイル単位の差分を対象とした。
- 判定: **APPROVE**

## R1 指摘の解消判定

| ID | 重大度 | 判定 |
|---|---|---|
| R1-1 | Critical | 解消 — `maybe_serve()` の `post_status` チェック直後に `post_password_required( $post )` を追加し、真の場合は通常テンプレートへフォールスルーするよう修正。回帰テスト `test_maybe_serve_skips_password_protected_post`（`go_to()` でパスワード保護記事の `?format=markdown` リクエストを再現し、出力が空であることを確認）で検証。PHPUnit 345件・PHPCS・PHPStan（`--memory-limit=3G`）すべてグリーン |

## 新規指摘

なし。修正はガード条件の追加のみで、既存の分岐順序（`enabled()` → `format`パラメータ →
`is_singular()` → `post_status` → `post_password_required()`）を壊しておらず、
新たな副作用は確認されなかった。

## 対象外指摘（差分範囲外）

なし（R1-L4 はbacklog記載済み、対応不要）。

## Low（backlogへ）

なし。
