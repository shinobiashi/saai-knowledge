# レビューバックログ（今回対応しない指摘）

| 追記日 | ID | 重大度 | 場所 | 内容 | 起票状況 |
|---|---|---|---|---|---|
| 2026-08-27 | R1-L1 | Low | plugins/saai-knowledge/includes/class-search.php | `saai_search_post_types` フィルターが複数の type key を同じ post_type にマッピングした場合、後勝ちで片方が無言で結果から欠落しうる | 未起票 |
| 2026-09-03 | R1-L2 | Low | plugins/saai-knowledge/includes/class-settings.php | `render_field()` が呼び出しごとに `defaults()`/`stored_settings()` を再計算（設定ページ1回の描画で7回）。管理画面専用・低頻度のため実害小 | 未起票 |
| 2026-09-03 | R1-L3 | Low | plugins/saai-knowledge/includes/class-settings.php | `slug_kb`/`slug_faq`/`slug_glossary` の重複チェックが3スラッグ間のみで、`knowledge-category` や既存ページスラッグとの衝突は未検証（WP一般的な制約として許容） | 未起票 |
| 2026-09-04 | R1-L4 | Low | plugins/saai-knowledge/includes/class-markdown-output.php | `render_cached()` のキャッシュキーは投稿自身の `post_modified` のみに依存するため、`saai_category` タームのリネームや `ai_readability_enabled` のOFF→ON切り替えでは、対象記事が再保存されるまでキャッシュ済みMarkdownの表記が古いまま残りうる（既存の自動リンク辞書キャッシュと同種のtradeoffとして許容） | 未起票 |
| 2026-09-05 | R1-L5 | Low | plugins/saai-knowledge/includes/class-markdown-converter.php | `to_plain_text()`（RAG export の `content_plain` 用）は既存の `fallback_plain_text()` と同じくHTMLエンティティをデコードしない。全文検索/embedding用途では「AT&amp;T」のように一部記号がエンティティ表記のまま残りテキスト品質がわずかに劣化しうる | [#49](https://github.com/shinobiashi/saai-knowledge/issues/49) |
| 2026-09-05 | R1-L6 | Low | plugins/saai-knowledge/includes/class-export.php | 管理画面の一括ダウンロード（JSONL/CSV）は `MAX_DOWNLOAD_ITEMS`(5000)件を一括でメモリに構築してからストリーミングする。R1-3でタイムアウト対策（`set_time_limit(0)`）は入れたが、根本的な「WP_Queryのページングに合わせて逐次出力する」設計への変更は将来の改善課題として残す | [#50](https://github.com/shinobiashi/saai-knowledge/issues/50) |
| 2026-09-06 | Codex-1 | Medium | plugins/saai-knowledge/includes/class-export.php | `saai_category`/`saai_tag` タームの名前のみを変更した場合、`wp_update_term()` は関連投稿の `post_modified_gmt` を更新しないため、`query_records()` の `modified_after` 増分同期はそのタームを持つ投稿を検知できず、`categories`/`tags` フィールドが古いまま残る（該当投稿が別の理由で再保存されるまで）。R1-L4（Markdown出力キャッシュの同種の既知トレードオフ）と同じ性質の問題として許容 | 未起票 |
| 2026-09-06 | Codex-2 | High | plugins/saai-knowledge/includes/class-export.php | 投稿の削除・非公開化（trash化・draft化）が `modified_after` 増分同期に一切通知されない。`query_records()` は `post_status => 'publish'` 固定のため、撤回された投稿は以後のレスポンスから静かに消えるだけで、削除を示すtombstoneも変更履歴もなく、RAG側は「変更なし」と「公開終了」を区別できず撤回済みの情報を提供し続けうる。ちゃんと対応するには削除履歴を記録する新規DBテーブルとスキーマ変更・保持期限ポリシー・エクスポートレスポンス形状の変更が必要で、本PRの他の修正より大きい設計変更のため、ユーザー確認のうえ次のIssueとして起票 | [#52](https://github.com/shinobiashi/saai-knowledge/issues/52) |
| 2026-09-06 | Codex-3 | High | plugins/saai-knowledge/includes/class-export.php | `page`（オフセットページング）と `orderby => modified ASC` の組み合わせは、複数ページの同期中に既取得投稿が更新されて並び順が変わると、間にある未取得投稿を永久に飛ばしうる。ちゃんと対応するには `page` を廃止し `(updated_at, ID)` ベースのキーセットページングへ切り替える必要があり、既にドキュメント化・テスト済みの `page`/`per_page`/`total_pages` というAPI契約を変える大きな変更になるため、ユーザー確認のうえ次のIssueとして起票（WordPress core の `/wp/v2/posts` も同じオフセットページングの制約を持つ） | [#53](https://github.com/shinobiashi/saai-knowledge/issues/53) |
| 2026-09-06 | Copilot-1 | Low | plugins/saai-knowledge/tests/test-llms-index.php | `test_maybe_remove_canonical_redirect_requires_exact_boundary_match`（318行目〜）と `test_maybe_remove_canonical_redirect_matches_a_non_default_priority`（407行目〜）は、`redirect_canonical` がテスト開始時点で未登録だった場合に一時的に `add_action()` する箇所があるが、後始末で「元々未登録だった」状態への復元をしておらず、登録されたまま残りうる。同種の指摘が新規テスト（`test_maybe_remove_canonical_redirect_matches_a_non_ascii_slug`）にも出て修正済みだが、この2件は本PRの差分範囲外のため未修正 | 未起票 |
| 2026-09-06 | R1対象外-1（issue-19） | Medium | plugins/saai-knowledge/includes/class-autolinker.php（`entry_excerpt()`） | `wp_trim_words( $post->post_content, 55 )` は、サイトのロケールが `wp_get_word_count_type()` で文字数カウント型を返す言語（例: `ja`）の場合、「55単語」ではなく「55文字」で切り詰める。日本語管理画面で英語の用語集定義を登録すると、自動リンクツールチップの抜粋が単語の途中で `&hellip;` 付きで切れる（例: "...passwor&hellip;"）。今回の余白修正の検証中に偶然発見 | [#56](https://github.com/shinobiashi/saai-knowledge/issues/56) |
| 2026-09-07 | PerfAudit-1 | Low〜Medium | plugins/saai-knowledge/includes/class-export.php（`build_record()`/`build_sections_via_dom()`） | KBタイプのレコードで同一の `content_html` を (a) `content_markdown` 生成（`Markdown_Output::render()` 内のDOM変換）、(b) `build_sections_via_dom()` のセクション分割、の2回別々にDOM解析している。REST側は `per_page` 上限200・`modified_after` 増分同期で通常は変更分のみのため実害限定的だが、初回フル同期や管理画面の一括ダウンロード（最大5000件、R1-L6/#50とは別論点）ではKB記事1件あたりの重複パースコストが積み重なる | 未起票 |
| 2026-09-07 | PerfAudit-2 | Low | plugins/saai-knowledge/includes/class-autolinker.php（`dictionary_for_context()`） | `process()` は投稿ごとに `sanitize_dictionary_entries()`（O(辞書サイズ)のサニタイズ）をフル実行する。一度「レンダリング結果キャッシュのヒット時は辞書計算をスキップする」順序変更を試みたが、Codex reviewで2ラウンドにわたり指摘された通り、`saai_autolink_dictionary` フィルターがcontext以外のリクエスト状態（`is_user_logged_in()`等）に応じて結果を変える場合に別リクエスト間でキャッシュが誤って共有される回帰があり、release-1.0.0-hardening branchで安全な元の順序（辞書計算→空チェック→キャッシュ参照）へ完全に差し戻し済み。この最適化自体を安全に実現するには、辞書計算結果を`$context`だけでなく完全なリクエスト状態で無効化する仕組みが必要で、コストに見合わないため見送り | 未起票 |
| 2026-09-07 | PerfAudit-3 | Low | plugins/saai-knowledge/includes/class-template-loader.php（`register_block_templates()`） | ブロックテーマ利用時、`init` のたびに同梱テンプレート7件をディスクから読み込み `register_block_template()` する（管理画面・REST・cronリクエストを含む全リクエストで発生）。ファイルは小さくOSキャッシュに乗るため実害は軽微 | 未起票 |
| 2026-09-08 | R1-X1（release-1.0.0-hardening） | 解消 | plugins/saai-knowledge/includes/class-sidebar-tree.php:283（`kb_posts_by_term()`）/ class-glossary-index.php（`items()`） | 当初 `Glossary_Index::items()` にのみ `MAX_ITEMS`(5000) キャップを追加し、`Sidebar_Tree::kb_posts_by_term()` は無制限のままという不揃いな状態を指摘したが、Codex reviewでそのキャップ自体が5001件超のサイトで用語を索引から恒久的に（回復不能に）欠落させる回帰だと判明し、`Glossary_Index::items()` も `posts_per_page => -1` へ差し戻した。両者とも「アーカイブは一ページ完結」という設計（DESIGN.md）上、意図的に無制限のままとする方が正しい。キャッシュ層の導入でリクエスト毎の実行は避けられているため、不揃いの解消をもって対応済みとする | 未起票 |
| 2026-09-08 | R1-L1（release-1.0.0-hardening） | Low | plugins/saai-knowledge/includes/class-glossary-index.php:261（`sort_key()`） | `bucket_from_folded()` 抽出（`bucket_for()`・`grouped_items_uncached()` がどちらも直接 `normalize()`+`fold_kana()` を呼ぶよう変更）により、クラス内外どこからも呼ばれなくなった公開メソッド（grep で呼び出し元ゼロを確認）。リファクタ後の死んだコード | 未起票 |
| 2026-09-08 | R1-L3（release-1.0.0-hardening） | Low | plugins/saai-knowledge/tests/test-kb-sidebar.php, tests/test-glossary-index.php | 一部の新規キャッシュ無効化テストだけが明示的に `->register()` を呼んでいるが、同じファイル内の兄弟テストは「`Plugin::boot()` が本番インスタンスを登録済みなので不要」と明記した上で呼んでいない。`flush_cache()` はidempotentなため実害は無いが、同一ファイル内で一貫性が無い | 未起票 |
| 2026-09-08 | Codex-R2-1（release-1.0.0-hardening） | Medium | plugins/saai-knowledge/includes/class-sidebar-tree.php（`kb_posts_by_term()`）/ class-llms-index.php（`collect_items()`） | 会員制プラグイン等が `pre_get_posts` で `saai_kb`/`saai_faq`/`saai_glossary` の取得対象を閲覧者の権限に応じて絞る環境では、サイト共通固定キーのキャッシュ（`Sidebar_Tree` および既出荷済みの `Llms_Index`）が特定の権限プロファイルのWP_Query結果を全訪問者へ共有しうる。安価に「現在の閲覧者の有効な可視性プロファイル」を識別する手段が無く、完全な解決には両クラスを含む設計変更が必要なため、ユーザー確認のうえ対応不要とした（本ラウンドのCodex reviewで指摘） | 未起票 |
| 2026-09-08 | Codex-R2-2（release-1.0.0-hardening） | Medium | plugins/saai-knowledge/includes/class-markdown-output.php（`render()`） | `Glossary_Index`/`Faq_List` と異なり、既出荷済みの `Markdown_Output::render_cached()` は同期パターン（`core/block`）を含む本文でも参照先 `wp_block` 投稿の更新をキャッシュ無効化のトリガーにしていない（`render()` が `do_blocks()` を経由するため同種の問題を抱える）。本ラウンドでは差分範囲外のため未修正。`Faq_List::render_answer()` の `has_block( 'core/block', $post )` によるキャッシュスキップと同じ対応が可能 | 未起票 |
| 2026-09-08 | Codex-R2-3（release-1.0.0-hardening） | Medium | plugins/saai-knowledge/includes/class-llms-index.php（`build_index_cached()`） | `Glossary_Index::grouped_items()` と同様、既出荷済みの `Llms_Index` も多言語サイト（WPML/Polylang）でサイト共通固定キーのままキャッシュしており、最初に表示した言語のタイトル・URLが他言語にも最大1時間漏洩しうる。本ラウンドでは差分範囲外のため未修正。`Glossary_Index::cache_key()` の `get_locale()` を含める対応と同じ手法が適用可能 | 未起票 |
